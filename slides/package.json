{
  "name": "hack-her-thon-trust-workshop",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base ./",
    "export": "slidev export --output workshop.pdf",
    "preview": "vite preview --host 127.0.0.1",
    "build:pages": "slidev build --base /hackhers-2026/ --router-mode hash",
    "predev": "python3 ../scripts/prepare-starter.py",
    "prebuild": "python3 ../scripts/prepare-starter.py",
    "prebuild:pages": "python3 ../scripts/prepare-starter.py",
    "preexport": "python3 ../scripts/prepare-starter.py"
  },
  "dependencies": {
    "@slidev/cli": "52.19.1",
    "@slidev/theme-default": "0.25.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "playwright-chromium": "^1.55.0"
  }
}
