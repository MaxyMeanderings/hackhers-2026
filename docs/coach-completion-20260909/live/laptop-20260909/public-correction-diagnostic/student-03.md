Still the same synthetic exercise. I revise my decision: drop reminders entirely and choose only a shared schedule view. We can now reach 7 members, and I will test with all 7. Their action is to confirm the updated rehearsal time from the shared view. Replace the previous threshold: success means at least 5 confirmations within 12 hours. This is a hypothesis, not proven demand, and the test has not run. Return only JSON with exactly these keys: demand_proven (boolean), real_interviews (use "Unknown"), feature_basis (use "assumption: a shared view may reduce missed changes"), result (use "not run"), threshold_count (number), metric (use "confirmations within 12 hours"), action (use "confirm updated rehearsal time"), participant_count (number), features (array containing only "shared schedule view"). No other keys.
