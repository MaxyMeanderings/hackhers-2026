Repair only docs/spec-fa7abd1282236c13/SPEC.md, preserving source requirements and nine file scope. Independent Codex adversarial review found these smallest-scope errors: research prohibitions cite wrong lines (source 79,81,82); anonymization source 51 and104 not44; .gitignore private/local-runs are lines11–12 not9–11; Model Router has 9 files under ONE top-level coach module, not four top-level areas. Claimed verbatim Test Plan transcription is false: paraphrases are allowed if labeled; fixture retains original case and expected behavior verbatim plus concrete runnable synthetic student prompts. Remove invented question about public evaluation summaries: task authorizes public sanitized artifacts; use synthetic test data and anonymized summaries, never real contact data. Approved source line106 requires a live run in selected local assistant, NOT mandatory live testing of both surfaces or time-based test waivers. Select authenticated Claude Code as live evaluation runtime; every required case and a full multi-turn scenario with all four deliverables must run. Keep Claude chat usage supported through same canonical prompt; check adapter points at identical canonical prompt and explicitly record Claude chat live validation as unverified when unavailable. Do not claim cross-surface live parity from same-prompt inspection. Remove mandatory browser run/fallback ambiguity and redundant grounding questions; preserve the parity behavior requirement. Live Claude Code auth was verified via claude auth status (claude.ai/firstParty). Browser at claude.ai currently shows sign-in. Do not need product decisions for evidence limitations. AC11 must require actual live ten case outcomes and source inspection; incomplete evidence never passes.
Use read-only shell excerpts, no full-file Read. No other files, commits, pushes, nested Codex or factory launchers. Return SUCCESS only if revised SPEC exists.
