# Coach completion drift review

Date: 2026-09-09

Decision: no material product-scope drift identified. This is a source and documentation review, not private final-proof acceptance.

## Authority and scope

The product authority is `docs/AGENT-SPEC.md`; the current task scope is `docs/coach-completion-20260909/SPEC.md`. The canonical prompt remains `coach/PROMPT.md`, with SHA-256 `c74966e3cb5c86801fe5d30aa410f6f14edb8a1ce755001a3b94d2fb4b2386e1`. The source preserves the six-stage inquiry, focused-question ceiling, independent count provenance, research limitations, anonymous participant handling, human decision and four deliverables. The prompt's narrower ordinary-reply and proposed-feature preferences remain within the product ceilings.

The chat instructions and optional terminal adapter reference the same prompt. They distinguish shared source from unverified live surface parity. The optional terminal remains subscription-based and requires no API key or paid integration. The evaluator's model remains configurable; Opus is the documented evaluated selection. Workshop slides and agenda remain separate work.

The four templates preserve idea evidence, six separate counts, one precommitted experiment, and a student-approved MVP of at most three features. The count-ledger wording now treats problem-report and contact provenance independently. Unknown values and synthetic versus real fieldwork remain distinct.

## Explicit evaluation-contract changes

`FORMAT-CONTRACT-AMENDMENT.md`, `FORMAT-CONTRACT-REVIEW.md` and `PROOF-CONTRACT-MIGRATION.md` record the authorized prospective format/model and cumulative-budget changes. These changes do not relabel earlier failures or reset consumed attempts. The result documentation retains historical failed tasks, intermediate trials and limitations. The private final proof is a separate required gate recorded in `FINAL-PROOF.md`.

## Evidence presentation

`coach/eval/RESULTS.md` attributes ten-case and full-conversation judgments to their independent reviews. Its actual response links identify the final prompt and evaluated runtime. The worked example reproduces the actual fifth response without semantic editing and links the five actual turns. It labels the exercise synthetic, retains Not run, and does not represent planned experiments as outcomes.

The final documentation does not claim real interviews, demand, live coach browsing or Claude chat parity. Wording caveats in the independent reviews remain disclosed rather than silently corrected in the recorded model output. Historical blocked checkpoints remain historical evidence, not the current product status. This review makes no new semantic certification of every retained transcript and does not inspect private heldout bodies.

Source identity for all 13 scoped product/configuration files is retained in `static-source-identity.json`. The helper's empty-environment model fallback and explicit blank-selector validation are resolved as recorded in `QA.md`.
