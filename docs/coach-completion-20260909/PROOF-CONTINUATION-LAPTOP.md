# Laptop proof continuation

The original heldout candidates remain exposed and ineligible. Their committed exposure and failed-review history remain unchanged. Fresh cases were authored independently from the specification and public scenarios, stored outside all checkouts and Git, and independently approved for classification and design. The private author inherited historical handoff context containing earlier prompt reads; no claim of complete historical source blindness is made. The author did not inspect the revised or frozen continuation prompt. Private case bodies have not been shared with the implementation author. The public commitment replaces the exposed commitment; this is not a final execution receipt.

The fresh private manifest covers B1 and B2 using two required cases and four model turns. Private bodies and locators are omitted. The public classification review identified an ambiguous contact count; the revised public input establishes exactly eight contacts and eight replies. Structural assertions do not establish semantic quality or full product acceptance; actual transcript review remains required.

The laptop runtime uses the recorded CLI version 2.1.263 (Claude Code). The runtime schema has no executable-path property; admission resolves the executable through PATH and records its physical path and hash. Calls use the verified local executable through /opt/homebrew/bin and subscription authentication. Hosted model aliases remain configurable and do not pin an immutable backend.

The canonical pre-seal ledger was restored byte-identically from proof-ledger-state.json only because no state existed. Its original failed challenge and budgets were retained. An empty state.lock was also required by the runtime's read-only transaction operation. Status then reported the original one development launch, zero final launches, zero repairs and zero infrastructure failures.

Fresh private commitment: `577bae8f935ab63fcc8583c5c1ca5b8e21f0e3fd447e2d43c0ac67393ce76333`.

## Provenance audit

The independent Codex reviewer `/root/coach_review` audited the disclosed public provenance. The specification and handoff require a separate independent author, fresh private cases outside checkouts and Git, actual independent classification review, a replaced commitment, and no implementation-author exposure of private bodies. They do not require absolute historical prompt blindness. Inherited historical prompt context does not by itself invalidate eligibility. This audit is conditional on the stated authorship, review and non-exposure facts; the provenance auditor did not inspect private bodies or authenticate unavailable prior reviewer turns. The original private classification evidence and commitment remain unchanged.
